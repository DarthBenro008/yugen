import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class ClimbPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/climb.png', fit: BoxFit.fitWidth),
    );
  }
}
